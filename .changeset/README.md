# Changesets

Run `nub run changeset` for every user-facing change. Select `lazydiff`, choose the semver bump, and describe the change for the generated changelog.

The release workflow keeps a version pull request up to date. Merging that pull request stages the package on npm and creates the matching GitHub release.
